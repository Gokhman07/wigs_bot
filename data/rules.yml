version: "3.1"
rules:
  - rule: Say hello
    steps:
      - intent: greet
      - action: utter_greet
  - rule: activate the default fallback action
    steps:
     - intent:  nlu_fallback
     - action:  action_default_fallback
